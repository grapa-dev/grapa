/* Test extending $ARRAY with new methods */
$ARRAY += op(x) { "Custom array method called with: " + x.str(); };

/* Check if the method was added */
"Testing extension system...".echo();
$ARRAY.echo();

/* Try to use the new method */
test_array = [1, 2, 3];
test_array.custom_method("hello");
