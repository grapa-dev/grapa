/* Minimal test */
testFunc = op(path) {
    $local.code = $file().get(path);
    if (code.type() == $ERR) {
        return "Error reading file";
    };
    return "File read successfully";
};

result = testFunc("../../../test/core/test_division_by_zero.grc");
result.echo();
