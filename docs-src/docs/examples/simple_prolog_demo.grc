#!/usr/bin/env grapa
/*
 * Simple PROLOG-style Knowledge Base Demo in Grapa
 */

"=== PROLOG Knowledge Base Demo ===".echo();

/* Initialize knowledge base */
kb = {}.table();

/* Store facts */
kb.setfield('fact1', 'parent(john,mary)');
kb.setfield('fact2', 'parent(mary,jane)');
kb.setfield('fact3', 'likes(mary,wine)');

"Stored Facts:".echo();
kb.getfield('fact1').echo();
kb.getfield('fact2').echo();
kb.getfield('fact3').echo();

/* Simple query - find all parent facts */
"".echo();
"Query: All parent facts:".echo();
keys = ['fact1', 'fact2', 'fact3'];
keys.range().map(op(i) {
    fact = kb.getfield(keys[i]);
    if (fact.indexOf('parent') == 0) {
        fact.echo();
    };
});

/* Variable binding example */
"".echo();
"Variable binding example:".echo();
$global.X = 'john';
$global.Y = 'mary';
"X=${X}, Y=${Y}".interpolate($global).echo();
"parent(${X},${Y})".interpolate($global).echo();

"=== PROLOG Demo Complete ===".echo();
