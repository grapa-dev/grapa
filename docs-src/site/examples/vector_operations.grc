/* Vector Operations Examples
 * 
 * This file demonstrates the full range of vector operations available in Grapa,
 * from basic creation and manipulation to advanced linear algebra and statistics.
 * 
 * Key features demonstrated:
 * - Vector creation and conversion
 * - Basic arithmetic operations
 * - Matrix operations and linear algebra
 * - Statistical functions with axis support
 * - Multi-dimensional operations
 * - Performance considerations
 */

("=== Vector Operations Examples ===").echo();

/* Basic Vector Creation and Conversion */

("--- Basic Vector Creation ---").echo();

// Create vectors from arrays
vec1 = [1, 2, 3].vector();
vec2 = [[1, 2], [3, 4]].vector();
vec3 = [[[1, 2], [3, 4]], [[5, 6], [7, 8]]].vector();

("1D Vector: " + vec1).echo();
("2D Vector: " + vec2).echo();
("3D Vector: " + vec3).echo();

// Convert back to arrays
arr1 = vec1.array();
arr2 = vec2.array();
("Converted back: " + arr1).echo();

// Shape information
("Vector shapes: " + vec1.shape() + ", " + vec2.shape() + ", " + vec3.shape()).echo();

/* Basic Arithmetic Operations */

("--- Basic Arithmetic ---").echo();

// Element-wise operations
a = [1, 2, 3].vector();
b = [4, 5, 6].vector();

sum = a + b;
diff = a - b;
product = a * b;
quotient = a / b;

("Sum: " + sum).echo();
("Difference: " + diff).echo();
("Product: " + product).echo();
("Quotient: " + quotient).echo();

// Scalar operations
scaled = a * 2;
shifted = a + 10;
("Scaled: " + scaled).echo();
("Shifted: " + shifted).echo();

/* Matrix Operations */

("--- Matrix Operations ---").echo();

// Create a 2x2 matrix
mat = [[1, 2], [3, 4]].vector();

// Basic matrix operations
transposed = mat.t();
inverse = mat.inv();
determinant = mat.det();
rank_val = mat.rank();

("Original matrix: " + mat).echo();
("Transpose: " + transposed).echo();
("Inverse: " + inverse).echo();
("Determinant: " + determinant).echo();
("Rank: " + rank_val).echo();

// Matrix structure operations
triu_mat = mat.triu();
tril_mat = mat.tril();
("Upper triangular: " + triu_mat).echo();
("Lower triangular: " + tril_mat).echo();

// 3x3 matrix for more complex operations
mat3 = [[1, 2, 3], [4, 5, 6], [7, 8, 9]].vector();
("3x3 matrix: " + mat3).echo();

// Triangular with offsets
triu_offset = mat3.triu(1);
tril_offset = mat3.tril(-1);
("Upper triangular (offset 1): " + triu_offset).echo();
("Lower triangular (offset -1): " + tril_offset).echo();

/* Linear Algebra Operations */

("--- Linear Algebra ---").echo();

// Dot product
vec_a = [1, 2, 3].vector();
vec_b = [4, 5, 6].vector();
dot_result = vec_a.dot(vec_b);
("Dot product: " + dot_result).echo();

// Matrix-vector multiplication
mat_2x3 = [[1, 2, 3], [4, 5, 6]].vector();
vec_3 = [1, 2, 3].vector();
mat_vec_result = mat_2x3.dot(vec_3);
("Matrix-vector product: " + mat_vec_result).echo();

// Matrix-matrix multiplication
mat_a = [[1, 2], [3, 4]].vector();
mat_b = [[5, 6], [7, 8]].vector();
mat_mat_result = mat_a.dot(mat_b);
("Matrix-matrix product: " + mat_mat_result).echo();

// Solve linear system (if matrix is square and non-singular)
// Note: This may return $ERR for singular matrices
square_mat = [[2, 1], [1, 3]].vector();
rhs = [5, 6].vector();
solution = square_mat.solve(rhs);
("Linear system solution: " + solution).echo();

/* Statistical Operations */

("--- Statistical Operations ---").echo();

// Create sample data
data_2d = [[1, 2, 3], [4, 5, 6], [7, 8, 9]].vector();

// Basic statistics
total_sum = data_2d.sum();
total_mean = data_2d.mean();
("Total sum: " + total_sum).echo();
("Total mean: " + total_mean).echo();

// Axis-based operations
sum_axis_0 = data_2d.sum(0);  // Sum along rows
sum_axis_1 = data_2d.sum(1);  // Sum along columns
mean_axis_0 = data_2d.mean(0); // Mean along rows
mean_axis_1 = data_2d.mean(1); // Mean along columns

("Sum along axis 0 (rows): " + sum_axis_0).echo();
("Sum along axis 1 (columns): " + sum_axis_1).echo();
("Mean along axis 0 (rows): " + mean_axis_0).echo();
("Mean along axis 1 (columns): " + mean_axis_1).echo();

// Covariance
cov_matrix = data_2d.cov();
("Covariance matrix: " + cov_matrix).echo();

/* Multi-dimensional Operations */

("--- Multi-dimensional Operations ---").echo();

// Create 3D data
data_3d = [[[1, 2], [3, 4]], [[5, 6], [7, 8]]].vector();
("3D data shape: " + data_3d.shape()).echo();

// Operations on different axes
sum_3d_0 = data_3d.sum(0);
sum_3d_1 = data_3d.sum(1);
sum_3d_2 = data_3d.sum(2);

("3D sum axis 0: " + sum_3d_0).echo();
("3D sum axis 1: " + sum_3d_1).echo();
("3D sum axis 2: " + sum_3d_2).echo();

// Reshaping
reshaped_2d = data_3d.reshape([4, 2]);
reshaped_1d = data_3d.reshape([8]);
("Reshaped to 4x2: " + reshaped_2d).echo();
("Reshaped to 1D: " + reshaped_1d).echo();

/* Advanced Matrix Operations */

("--- Advanced Matrix Operations ---").echo();

// Eigenvalues and eigenvectors (for symmetric matrices)
symmetric_mat = [[2, 1], [1, 3]].vector();
eigen_result = symmetric_mat.eigh();
("Eigenvalues/eigenvectors: " + eigen_result).echo();

// Reduced row echelon form
rref_mat = [[1, 2, 3], [4, 5, 6], [7, 8, 9]].vector();
rref_result = rref_mat.rref();
("RREF: " + rref_result).echo();

/* Performance Examples */

("--- Performance Examples ---").echo();

// Large vector operations
large_vec = (1000).range().vector();
("Large vector size: " + large_vec.shape()).echo();

// Timing operations
start_time = time();
large_sum = large_vec.sum();
end_time = time();
("Large vector sum time: " + (end_time - start_time) + " seconds").echo();

// Matrix operations on larger data
large_mat = (100).range().reshape([10, 10]).vector();
start_time = time();
large_det = large_mat.det();
end_time = time();
("Large matrix determinant time: " + (end_time - start_time) + " seconds").echo();

/* Error Handling Examples */

("--- Error Handling ---").echo();

// Try to convert irregular array (should fail)
irregular = [[1, 2], [3]];
("Attempting to convert irregular array...").echo();
irregular_vec = irregular.vector();
if (irregular_vec.type() == $ERR) {
    ("✅ Correctly rejected irregular array").echo();
} else {
    ("❌ Unexpectedly accepted irregular array").echo();
}

// Try to solve with singular matrix
singular_mat = [[1, 1], [1, 1]].vector();
singular_rhs = [1, 2].vector();
singular_solution = singular_mat.solve(singular_rhs);
if (singular_solution.type() == $ERR) {
    ("✅ Correctly handled singular matrix").echo();
} else {
    ("❌ Unexpectedly solved singular matrix").echo();
}

/* Real-world Examples */

("--- Real-world Examples ---").echo();

// Image processing simulation (2D data)
image_data = (64).range().reshape([8, 8]).vector();
("Image data (8x8): " + image_data).echo();

// Apply a simple filter (average of neighbors)
filtered = image_data.triu(-1).tril(1);  // Keep main diagonal and adjacent
("Filtered image: " + filtered).echo();

// Data analysis example
sales_data = [[100, 150, 200], [120, 180, 220], [90, 140, 190]].vector();
("Sales data: " + sales_data).echo();

// Calculate statistics
total_sales = sales_data.sum();
avg_sales = sales_data.mean();
sales_by_period = sales_data.sum(1);  // Sum across products
sales_by_product = sales_data.sum(0); // Sum across periods

("Total sales: " + total_sales).echo();
("Average sales: " + avg_sales).echo();
("Sales by period: " + sales_by_period).echo();
("Sales by product: " + sales_by_product).echo();

/* Best Practices */

("--- Best Practices ---").echo();

("✅ Use .vector() for mathematical operations").echo();
("✅ Use .array() for general data manipulation").echo();
("✅ Specify axis parameter for multi-dimensional operations").echo();
("✅ Check for $ERR when operations may fail").echo();
("✅ Use reshape() for changing dimensions").echo();
("✅ Consider memory usage for large vectors").echo();

("=== Vector Operations Examples Complete ===").echo();
