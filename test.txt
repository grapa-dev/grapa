./grapa -c "model = \$MODEL(); model.load('gpt-4o', 'openai'); result = model.gen('The quick brown fox jumps over the lazy dog', {'api_key': openai_key}); result.echo();"

import grapapy
g = grapapy.grapa()
g.eval('model = $MODEL(); model.load("gpt-4o", "openai");')
g.eval('model.info();')	
g.eval('model.gen("The quick brown fox jumps over the lazy dog", {"api_key": openai_key});')	
        
import grapapy
grapapy.eval('model = $MODEL(); model.load("gpt-4o", "openai"); model.gen("what is your favorate color?", {"api_key": openai_key});')
