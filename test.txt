./grapa -c "model = \$MODEL(); model.load('models/all-MiniLM-L6-v2-onnx/model.onnx', 'onnx-embedding'); result = model.gen('The quick brown fox jumps over the lazy dog'); result.echo();"

import grapapy
g = grapapy.grapa()
g.eval('model = $MODEL(); model.load("models/all-MiniLM-L6-v2-onnx/model.onnx", "onnx-embedding");')
g.eval('model.info();')	
g.eval('model.gen("test");')	
  