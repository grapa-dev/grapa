matichuk@Chris-MacBook-Pro grapa % ./grapa docs-src/docs/examples/model_interpolate.grc
Model loaded successfully
Prompt: What is 15 * 23 + 45? Please show your work step by step using ${expression} format for all calculations.
Expected format: '15 * 23 = ${15 * 23}' and '345 + 45 = ${345 + 45}'
Raw LLAMA response:
 To solve the expression \(15 \times 23 + 45\), we will break it down into steps and use the ${expression} format for each calculation.

1. First, calculate \(15 \times 23\):
   \[
   15 \times 23 = ${15 * 23}
   \]
   Evaluating the expression:
   \[
   15 \times 23 = 345
   \]

2. Next, add 45 to the result from the first step:
   \[
   345 + 45 = ${345 + 45}
   \]
   Evaluating the expression:
   \[
   345 + 45 = 390
   \]

Therefore, the final answer is:
\[
15 \times 23 + 45 = ${390}
\] 

So
After interpolation:
 To solve the expression \(15 \times 23 + 45\), we will break it down into steps and use the ${expression} format for each calculation.

1. First, calculate \(15 \times 23\):
   \[
   15 \times 23 = 345
   \]
   Evaluating the expression:
   \[
   15 \times 23 = 345
   \]

2. Next, add 45 to the result from the first step:
   \[
   345 + 45 = 390
   \]
   Evaluating the expression:
   \[
   345 + 45 = 390
   \]

Therefore, the final answer is:
\[
15 \times 23 + 45 = 390
\] 

So
matichuk@Chris-MacBook-Pro grapa % 